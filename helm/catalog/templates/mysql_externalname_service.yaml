apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.database.serviceName }}
spec:
  type: ExternalName
  externalName: {{ .Values.database.endpoint }}
  ports:
    - port: {{ .Values.database.port }}
      
