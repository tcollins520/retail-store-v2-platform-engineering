apiVersion: v1
kind: Service

metadata:
  name: {{ include "ui.fullname" . }}-canary

spec:
  type: ClusterIP

  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http

  selector:
{{ include "ui.selectorLabels" . | nindent 4 }}